- Display CAN Messages
- Buttons for storing the Log (start / stop)
- connection with log service 
- logging functionality

Optional 
- update slider if over 50% -> needs some kind of hook or listener for the vss data
- send can messages for fan speed periodically -> implement in monitor service
